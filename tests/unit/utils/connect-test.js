import connect from 'dummy/utils/connect';
import { module, test } from 'qunit';

module('Unit | Utility | connect', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = connect();
    assert.ok(result);
  });
});
